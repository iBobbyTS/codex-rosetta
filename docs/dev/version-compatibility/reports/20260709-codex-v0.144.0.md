# Codex v0.144.0 Upgrade Report and Execution Plan

Status: **Pending / not approved**. Implementation and automated checks have partial evidence, but the required live matrix is incomplete. The source version remains `0.144.0.r0`; this report does not authorize a release.

## Upgrade baseline

| Item | Before Upgrade | Target/After Upgrade |
| --- | --- | --- |
| Codex CLI releases | `codex-cli 0.142.5` | `codex-cli 0.144.0` |
| Codex release tag/commit | Not documented separately | `rust-v0.144.0` / `767822446c7a594caa19609ca435281a9ec67e0d` |
| Codex source branch | `main` | `main`, consistent with `origin/main` |
| Codex source commit | `cca16a10878202cb2f6e9666b6b4330329ea7e65` | `2e8c3756f95789c215d9ea9a5ade6ec377934b3f` |
| Codex source commit time | `2026-07-06T21:22:51-07:00` | `2026-07-09T11:53:18-07:00` |
| Codex source commit title | `feat(core): emit canonical command execution items (#31297)` | `build: ratchet direct reqwest dependencies (#31431)` |
| Codex-Rosetta package version | `0.142.5` | `0.144.0.r0` (the first Rosetta patch of Codex `0.144.0`) |
| Codex-Rosetta revision | `419e6f90ed39a97b1da2e3a7a4704385ff76d9fc` | Audited dirty snapshot at HEAD `eb947426572ad7658c4b5ad19688fa68659a06b6`; no clean release revision has been identified |
| Inspection date | `2026-07-09` | `2026-07-09` |

Before the upgrade, `../openai-codex-src` was confirmed to have no uncommitted tracked changes. The pre-upgrade contract check matched the saved `cca16a108782…` baseline: 9 high-confidence unchanged items, 12 possibly unchanged items, and 0 changed items.

The Codex-Rosetta side was not a clean release candidate when the fourth-round audit inspected it. HEAD was `eb947426572ad7658c4b5ad19688fa68659a06b6`, with 74 tracked modified files, 14 untracked files, and a tracked diff of 3,358 additions / 862 deletions. These values identify a dirty review snapshot only; they do not bind the evidence to a reproducible release commit. Approval requires a future exact clean Codex-Rosetta commit plus completion of every triggered live gate.

## Source Synchronization Results

After confirming that the source checkout had no tracked modifications, `git fetch origin` and `git pull --ff-only` fast-forwarded it from `cca16a1…` to `2e8c375…`. The synchronization touched 558 files without resetting, stashing, or overwriting local work.

The peeled commit of `rust-v0.144.0` is `7678224…`; the synchronized `main` has 21 more commits than the release commit. Therefore, this report also retains: target package version `0.144.0.r0`, release commit and the latest source code commit actually reviewed. The classification is based on the more conservative `cca16a1…2e8c375` full range; the new `/realtime/calls` and code-mode deferred MCP descriptions belong to the `main` changes after the release tag.

## Contract group classification

### High-Confidence Unchanged

- `contract.apply_patch`
- `contract.codex_header_constants`
- `contract.responses_metadata_keys`
- `contract.sse_event_names`
- `contract.transport_constants`
- `contract.websocket_client_metadata_keys`

### Possibly Unchanged

- `contract.compaction_input_fields`
- `contract.content_item_variants`
- `contract.message_phase_variants`
- `contract.model_enum_variants`
- `contract.model_info_fields`
- `contract.reasoning_fields`
- `contract.response_create_ws_request_fields`
- `contract.response_event_variants`
- `contract.response_input_item_variants`
- `contract.response_item_variants`
- `contract.responses_api_request_fields`
- `contract.stream_options_fields`

These contract groups prove only name/member-set consistency; they do not yet fully cover types, defaults, serde behavior, or runtime semantics.

### Changed

- `codex_source_commit`: `cca16a108782…` → `2e8c3756f957…`
- `contract.endpoints`: added `REALTIME_CALLS_ENDPOINT=/realtime/calls`
- `contract.tool_spec_wire_types`: Remove built-in `image_generation=ImageGeneration`

Additionally, a manual source diff found changes not yet covered by the current extractor:

- hosted web search field changed from `index_gated_web_access` to `indexed_web_access`;
- `ModelMessages` adds model-specific approval messages;
- `models.json` adds 5.6 models with `use_responses_lite=true`, `code_mode_only` and multi-agent enabled, and adds `max/ultra` reasoning;
- Responses Lite puts tools into `input[].type=additional_tools` instead of top-level `tools`;
- `bio_policy` is now handled by Codex as a terminal invalid request;
- Changes in stream item ID, canonical TurnItem persistence, compact model fallback and MCP tool snapshot behavior.

## Compatibility-Point Classification and Plans

The 16 compatibility points were classified as follows:

- **High-confidence unchanged (2)**: Responses direct passthrough; request and window identity;
- **Possibly unchanged (3)**: Code tool localization, Codex tool usage tips, Message phase;
- **Changed (11)**: Agent-facing API, Responses→Chat bridge, Responses Lite / `additional_tools`, custom/freeform tools, tool-history consistency, deferred tool discovery, web-search bridge, stream lifecycle, reasoning, context-compaction resilience, and model-level switches.

| Compatibility points | Classification | Source code/contract evidence | Fix or review plan | Automation results | Evidence status | Real API evidence / gap |
| --- | --- | --- | --- | --- | --- | --- |
| Agent-facing API | Changes | 0.144.0 catalog activates Lite/code mode; latest `main` adds realtime route; custom provider default WS is still closed | Keep Rosetta limited to HTTP/SSE; capture real transport/body behavior | Contract endpoint changes; special project not executed | **Tested:** controlled third-party HTTP/SSE route. **Unsupported:** Responses WebSocket, incremental history, remote compact. **Unverified:** native GPT route | Controlled `deepseek-v4-flash` run only; native GPT request capture is missing |
| Responses are transmitted as is | High-confidence unchanged | HTTP request/event set remains unchanged; raw direct path can retain unknown fields | Add Lite body, `bio_policy` SSE and unknown-field byte-for-byte fixtures | Contract request/event set is consistent; special project not executed | **Unverified / not triggered** | The controlled bridge run is not direct same-format Responses evidence |
| Request and window identity | High-confidence unchanged | Metadata/header key has not changed; window remains `{thread_id}:{window_number}` | Add header/body snapshot and compact/fork/resume fixture | Metadata/header contract is consistent; special project not executed | **Unverified / not triggered** | Real turn/compact/resume/fork/subagent capture is missing |
| Responses→Chat bridge | Changes, repaired | Bridge extracts Lite tools/developer instructions, deduplicates tools, and preserves history metadata | Retain merge/restoration regression coverage | Converter/gateway Lite and history regressions pass | **Tested:** controlled third-party bridge path. **Unverified:** native GPT path | Lite multi-turn read/write/diff completed through `deepseek-v4-flash` |
| Responses Lite / `additional_tools` | Changes, repaired | Bridge handles embedded tools, developer instructions, and image-generation filtering | Preserve direct and downgrade fixtures | Automated embedded-tool and filtering fixtures pass | **Tested:** controlled third-party Lite path | Multi-turn tool run consumed subsequent tool results through `deepseek-v4-flash` |
| custom/freeform tools | Changes, repaired | `exec` is restored as `custom_tool_call` with raw JavaScript input | Retain non-streaming and stream lifecycle fixtures | Responses converter custom/freeform regressions pass | **Tested:** controlled `exec` success. **Unverified / not triggered:** malformed `exec` recovery and nested wait | Controlled code-mode run exited 0 through `deepseek-v4-flash`; negative shape was not reproduced |
| Code tool localization | Possibly unchanged | Classic localization remains; code-mode `exec` stays custom | Retain classic and custom/freeform tests | Non-streaming and streaming fixtures pass | **Tested:** controlled file read/write/diff and `exec`. **Unverified:** malformed recovery | Evidence is limited to the third-party route |
| Tool history consistency | Changes, repaired locally | Canonical TurnItem, stream IDs, fork history, and MCP snapshot changed; Rosetta restart replay now uses exact AES-256-GCM-protected SQLite mappings instead of redacted operational state | Verify compact/resume/fork/restart pairing, matched DB/key restore, fail-closed key lifecycle, and isolation | Encrypted at-rest/exact replay, same-process/restart, tamper/wrong/missing key, legacy migration, TTL and principal isolation fixtures pass | **Unverified / not triggered** | Compact/resume/fork/restart live sequence is still missing |
| Deferred tool discovery | Changes | `image_gen` namespace and deferred MCP shared types changed | Verify namespace defer, repeated search, and dual windows | Tool wire contract changed; special project not implemented | **Unverified / not triggered** | Real plugin/MCP search, call, result consumption, and image generation are missing |
| Codex tool usage tips | Possibly unchanged | `request_user_input` schema appears unchanged; Desktop tool payloads remain runtime-owned | Add native schema and Desktop fixtures | Prompt text is outside the extractor; special project not executed | **Unverified / not triggered** | Real `request_user_input`, Goal/Plan, and Desktop tool calls are missing |
| Web search bridge | Changes | Hosted field renamed; standalone search moved to extension-owned item | Retain both hosted and namespace search paths | Field semantics are not extracted; special project not executed | **Unverified / not triggered** | Real search, recovery, continuation, and compact/resume are missing |
| Stream lifecycle | Changes, repaired locally | Terminal invalid-request behavior and item ID completion changed; converted/raw/web-search generators now distinguish completed, provider error, and cancellation/early close in trace/profile output | Verify one terminal, consistent added/done/completed IDs, and real client disconnect | Event names match; local early-close regressions cover all three stream paths | **Tested:** controlled successful stream. **Unverified / not triggered:** real disconnect and changed error paths | Controlled runs showed one terminal; real disconnect and safe invalid-parameter paths remain missing |
| Message phase | Possibly unchanged | Enum unchanged; extension items and IDs add risk | Add extension/completed-only fixtures and UI check | Phase enum matches; special project not executed | **Unverified / not triggered** | Codex UI commentary/final, mailbox/steering, and tool order are missing |
| Reasoning | Changes, repaired in fixtures | 5.6 enables `max/ultra`; Lite uses `context=all_turns` | Retain `ultra` mapping and define remaining context downgrade | Automated mapping coverage exists | **Tested:** controlled `ultra` route. **Unverified:** full pre/post-tool continuation | Controlled DeepSeek route is not native GPT reasoning evidence |
| Context compaction resilience | Changes | Compact fallback, item IDs, and catalog compaction behavior changed | Verify fallback, orphan cleanup, resume/restart, and alias switch | Field set matches; behavioral special project not executed | **Unsupported:** remote compact. **Unverified / not triggered:** local compact/resume sequence | Required long-session compact/resume/restart/model-switch run is missing |
| Model-level switches | Changes | Lite, code-mode, multi-agent, approvals, and reasoning levels changed | Verify each actual model switch and fallback independently | Field names match; values/nested semantics are not fully covered | **Tested:** controlled Lite/code-mode override. **Unverified / not triggered:** native catalog and multi-agent | Controlled alias cannot be counted as native GPT or multi-agent evidence |

## Execution phase

### Phase 1: Enhance source code change detection

1. Extend the extractor to capture the complete web-search tool field, `AdditionalTools` wire shape, `ModelMessages.approvals` and the key capability values of the bundled model.
2. Reserve independent fields for tag commit, synchronized source commit and package release version.
3. Add extractor fixture to ensure that field rename, nested schema and catalog value changes enter "changed".

### Phase 2: Blocking compatibility fixes completed

1. Responses Lite request parsing now recovers tools, developer instructions, history and reasoning context, with defined top-level/embedded deduplication.
2. Image-generation filtering recognizes the old and namespaced forms in top-level and `additional_tools` definitions.
3. The web-search bridge and terminal error contract have regression coverage.
4. `ultra` reasoning and the provider downgrade path are covered by automated and controlled live tests recorded below.

### Phase 3: Tightening history, deferred tools and streams

1. Add deterministic regression to the bridge's item ID, call ID, added/done/completed associations.
2. Cover the state isolation of canonical/extension TurnItem, MCP snapshot, image namespace, compact/resume/ fork/restart and two windows.
3. Only when the header life cycle changes are actually captured, the window key is extended from header to body `client_metadata`; state ownership is not changed based on guessing.

### Phase 4: Run All Automated Gates

1. Run the Codex special pytest, including the new Lite/catalog/web-search/error/history fixtures added in this report.
2. Run `make lint` and `make test`.
3. `make check-codex-compat` passes against the reviewed `2e8c3756f957…` baseline after semantic review and fixes completed.

### Phase 5: Real Codex/API Testing

1. Use `deepseek-v4-flash` by default; use controlled model catalog override for Lite/code-mode to ensure that the target wire shape is actually triggered instead of just testing the unknown-model fallback.
2. Merge execution text/tool/Lite, patch error recovery, deferred plugin/MCP, web search, phase, reasoning, compact/resume/fork and concurrent window scenarios, and backfill the results in the above table one by one.
3. Do not trigger sensitive real prompts for `bio_policy`; use fixtures instead. Exercise live error gates with safe invalid parameters or model paths and record why they were substituted.

### Phase 6: Closing and version upgrade

After all fixed, automated and triggered real tests pass, update the contract baseline, this report, the current compatible baseline and version, and upgrade the Codex-Rosetta package version from `0.142.5` to `0.144.0.r0`. The baseline records the actual review of `2e8c375…`, while retaining the release tag commit `7678224…`.

## Automated test results

Pre-upgrade and post-synchronization contract checks have been run this round. The post-sync check blocks as expected, reporting 6 high-confidence unchanged contract groups, 12 likely unchanged groups, and 3 changed items. It's just a source code diff gate, not a test failure.

- Codex special matrix: `404 passed`;
- Directed extended regression: `425 passed, 6 warnings`;
- custom/freeform regression: `tests/converters/openai_responses`, `356 passed`;
- `make lint`: passed;
- `make test`: `2326 passed, 4 skipped, 27 warnings`.

Remaining gaps include native GPT, malformed `exec` recovery, compact/resume/fork, plugin/MCP/deferred tools, web search, UI phase, request-user-input/Desktop tools, changed error paths, and multi-agent continuation. Under the repository's mandatory rule, every compatibility point classified as possibly unchanged or changed needs its triggered real Codex/API evidence before approval. Automated checks and the controlled DeepSeek alias do not close those gaps.

## Real API test results

Using an isolated gateway at `127.0.0.1:18772`, the controlled Lite/code-mode catalog completed short-answer, file-read, multi-turn read/write/diff, `ultra`, and code-mode `exec` regressions. Every completed stream contained exactly one source-side and one downstream `response.completed`, plus `finish` and `stream_end` trace records.

For this `exec` run, the Codex-facing model was `gpt-5.6-terra`, but the trace confirms that the actual route was `deepseek-v4-flash`. It therefore provides evidence only for the third-party bridge/code-mode path, not for native GPT requests. The CLI exited with status 0 after successfully reading the first line of `README.md`; the session is `019f48fe-1be3-7751-b0ff-4a9a6cc639f1`, and the trace is `/Volumes/RAM Disk/codex-0144-deepseek-20260709.jsonl`. This model did not reproduce the invalid `exec({"cmd": ...})` shape, so that negative case remains required in future model regressions.

## Upgrade decision

**Pending / not approved.** The controlled `deepseek-v4-flash` alias is valid evidence for the tested third-party bridge, Lite/code-mode, and `exec` paths only; it is not native GPT evidence. The remaining triggered live scenarios listed above are mandatory, and the audited Codex-Rosetta tree is a dirty snapshot rather than a clean reproducible revision.

The existing source version `0.144.0.r0` and historical/manual tag spelling `v0.144.0.r0` are not changed by this correction, but neither is release authorization. Approval may be recorded only after all triggered live scenarios pass against an exact clean Codex-Rosetta commit and this report is updated atomically with that evidence.
