Run `python3 scenario.py` exactly once. Do not read the script or any other file
to infer its output. After the command finishes, reply with only one compact
JSON object using exactly these string keys and the values from the command
output: `project`, `stage`, `ledger_policy`, `timezone`, `endpoint`,
`predeploy_check`, `reference_code`. Do not add Markdown or an evaluation.
