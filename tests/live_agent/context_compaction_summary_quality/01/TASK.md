Run `python3 scenario.py` exactly once. Do not read the script or any other file
to infer its output, and do not run any other command. After the command
finishes, reply with only `PHASE1:QUALITY_CONTEXT_READY`.
