First phase: reply only `FIRST:SWITCH-NONCE-7Q4M`. The test executor resumes the
thread with the executor-selected default native GPT model and a prompt to
proceed with the resume phase. On resume, reply only
`RESUMED:DEEPSEEK_TO_GPT:SWITCH-NONCE-7Q4M`.
