Call `view_image` exactly once with path `fixtures/quadrants.png`. You may omit
`detail` or choose any value allowed by the visible tool schema. Do not read the
file, convert it, or use another image or file tool.

If `view_image` returns a non-error image result, reply with only
`RESULT:VIEW_IMAGE_OK`. Otherwise reply with only `RESULT:VIEW_IMAGE_FAILED`.
